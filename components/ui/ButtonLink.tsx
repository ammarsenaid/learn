import Link from 'next/link';
export default function ButtonLink({ href, children, variant = 'secondary', className = '' }: { href: string; children: React.ReactNode; variant?: 'primary'|'secondary'; className?: string }) { const styles = variant === 'primary' ? 'bg-[#f3c76a] text-[#03111f] border-[#f3c76a]' : 'border-sky-400 text-sky-200'; return <Link href={href} className={`inline-flex rounded-lg border px-4 py-2 text-sm font-semibold transition hover:opacity-90 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-sky-300 ${styles} ${className}`}>{children}</Link>; }
